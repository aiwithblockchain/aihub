/**
 * TweetClaw global defaults
 * Change these constants to update all connection defaults at once.
 */
export const DEFAULT_WS_HOST = '127.0.0.1';
export const DEFAULT_WS_PORT = 20086;
export const DEFAULT_REST_PORT = 20088;
